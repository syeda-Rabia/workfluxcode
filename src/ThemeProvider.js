// for gobal blue color of the material ui button
import { createMuiTheme } from "@material-ui/core/styles";
var blue = "#2B7AE4";
export const theme = createMuiTheme({
  palette: {
    primary: {
      main: blue,
    },
  },
});
